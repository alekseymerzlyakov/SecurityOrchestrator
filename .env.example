# Server settings
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database
# DB_PATH is calculated relative to BASE_DIR by default
# but can be overridden here if needed
# DATABASE_URL=sqlite+aiosqlite:///data/aiso.db
